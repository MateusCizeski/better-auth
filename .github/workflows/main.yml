      - name: 🚀 Deploy no servidor
        run: |
          ssh -o StrictHostKeyChecking=no \
              -o ProxyCommand="cloudflared access ssh --hostname %h" \
              mateus@ssh.meuservidorubuntu.com.br "
              if [ ! -d /var/www/better-auth ]; then
                git clone git@github.com:seu-usuario/seu-repo.git /var/www/projeto
              else
                cd /var/www/projeto && git pull origin main
              fi
              "
